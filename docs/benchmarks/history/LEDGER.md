# Performance history ledger

Each row is one `bench-history.sh` run. The raw numbers live in the
`NNNN__<label>__<commit>.txt` file; the benchstat comparison against the
previous run lives in the matching `.delta.txt`. Fill the **Summary** column
with the one-line outcome of the change (the headline delta from the
`.delta.txt`), so the table reads as a chronological record of gains and
regressions.

| Seq | Date (UTC) | Commit | Label | Summary |
|----:|-----------|--------|-------|---------|
| 0001 | 2026-05-29 | `1634256` | baseline | Reference point. IC1 408µs / 588 KiB / 10794 allocs; IC2 135µs / 3641 allocs; IC9 165µs; IC10 232µs. Graph algos unchanged throughout. |
| 0002 | 2026-05-29 | `1634256-dirty` | opt1-mapper-shardfor-unsafe | **Gain.** Eliminated string interface-boxing in `Mapper.shardFor`. cypher_ldbc geomean **−4.63% time, −14.86% allocs**; IC1 −6.15% time / −18.53% allocs (10794→8794); IC2 −6.18%/−18.35%; IC5 −6.52%; IC9 −4.41%/−18.38%; IC10 −2.80%. Graph-algo guard band flat (no regression). TCK 3897/3897, race-clean. |
| 0003 | 2026-05-29 | `be3c018-dirty` | opt2-nodeid-accessors | **Gain (time).** NodeID-keyed lpg accessors skip 2 redundant Mapper `Lookup`s per node (identity `Resolve` kept). cypher_ldbc geomean **−9.43% time** (allocs flat): IC1 −14.80% (383→326µs), IC2 −16.40%, IC9 −12.56%, IC10 −10.42%. Writes/algos flat. TCK 3897/3897, race-clean. _Cumulative baseline→0003: IC1 −20% time / −18.5% allocs._ |
| 0004 | 2026-05-29 | `9c12628-dirty` | opt3-skip-empty-propmap | **Gain.** Skip `make(expr.MapValue)` for propertyless nodes (nil map reads identically). cypher_ldbc geomean **−3.63% time, −6.98% allocs, −4.46% bytes**: IC1 −11.37% allocs (8794→7794, exactly 1/node), IC2 −11.23%, IC9 −11.26%, IC10 −7.19%. Writes/algos flat. TCK 3897/3897, race-clean. _Cumulative baseline→0004: IC1 −23.7% time / −27.8% allocs; IC2 −26.3% time / −27.5% allocs._ |
| 0005 | 2026-05-29 | `7f6c1b5-dirty` | opt4-materialize-takerecord | **Gain (time).** `materialize` takes ownership of the ResultSet's row map (TakeRecord installs a fresh one) instead of copying — each row's data is now touched once, not twice. Barrier semantics unchanged; allocs flat. cypher_ldbc geomean **−6.28% time**: IC1 −15.16% (311→264µs), IC2 −14.92%, IC10 −5.96%. Writes/algos flat. TCK 3897/3897; race-clean incl. txn/recovery/cypher-isolation. _Cumulative baseline→0005: IC1 −35.3% time / −27.8% allocs; IC2 −37.2% time._ |
| 0006 | 2026-06-14 | `f6f8c7a` | baseline-perfcycle-2026-06-14 | **Reference point for the 2026-06-14 perf-audit cycle (rmp S-PA1..S-PA6, #1497-1525).** IC1 287962 ns/op / 522270 B/op / 7795 allocs/op; IC2 96291 ns / 171794 B / 2640 allocs; IC5 5592 ns / 41 allocs (write path); ProjectOp 12793 ns / 22472 B / 762 allocs. Guard band: Dijkstra_Large 8.71m / 1.089Mi / 4 allocs; BFSDirectionOpt_PowerLaw 29.56m / 0 alloc; Brandes_RandomGraph 8.03m / 94.34Ki / 1921 allocs. Every S-PA* change is benchstat-gated against this row; the guard-band group must stay `~`. TCK 3897, -race clean at capture. |
| 0007 | 2026-06-14 | `360c301-dirty` | opt-soa-result-rows | **Big gain (time + memory).** Column-oriented (SoA) materialised result rows (#1499): `materialize` now copies each row's positional values into one flat `[]expr.Value` backing slice instead of allocating a `map[string]Value` per row; the public per-row map is built lazily into a single reused scratch only when `Record()` is called, and the Bolt PULL path reads values positionally via the new `Result.ValueAt`. cypher_ldbc geomean **−13.17% time / −28.37% B/op**: IC1 **−32.4% time / −60.9% B/op / −25.6% allocs** (510→199 KiB), IC2/IC3/IC7/IC14 ≈ −30% time / −60% B/op / −25% allocs, IC10 −14.7% time. cypher_alloc ResultSet **−14.04% time**; ProjectOp/AllNodesScan `~`. Tiny single-row queries (IC5/6/8/12/13) +1 alloc (the reused scratch map) — within noise of the pinned targets, all of which improve. Guard band `~` (controlled A/B count=10: BFS p=0.063, Dijkstra_PostWarmup p=0.315, Dijkstra_Large/Brandes flat; the history-run +1.3% was thermal noise — graph algos never touch the result path). TCK 3897/3897; -race clean across cypher/store/bolt. _Cumulative baseline→0007: IC1 ≈ −56% time vs 0001._ |
| 0008 | 2026-06-14 | `4173f8b-dirty` | opt-soa-result-rows-fix | **Regression fix — zero regression everywhere (#1499 follow-up).** 0007's SoA materialisation added +1 alloc / +~4% B/op on tiny single-row queries (IC5/6/8/12/13) from two eagerly-allocated maps a positional drain never reads: the `recScratch` map in `materialize()` and the `ResultSet.current` map in `exec.Run`. Both are now allocated lazily on the first `Record()`/`TakeRecord()` call. cypher_ldbc vs **0006 baseline**: IC5/IC8 **41→40 allocs** (−1), IC6 42→41, IC12/IC13 **51→50** — all now **below** baseline; B/op back to `~` or lower; small-query time −1.0…−2.3%. The 0007 wins are fully preserved: IC1 −25.7% allocs / −60.9% B/op, IC2/IC3/IC7/IC14 ≈ −25% allocs / −60% B/op, IC10 −14.4%. cypher_alloc ResultSet −16.4% time / −2 allocs; AllNodesScan/ProjectOp flat. Guard band (Dijkstra/BFS/Brandes) `~` (allocs/B/op identical, sec/op within ±2% noise). **Net effect: every curated benchmark ≤ baseline 0006 on allocs AND B/op; none worse.** TCK 3897/3897; -race clean across cypher/store/bolt; golangci/staticcheck clean. |
| 0009 | 2026-06-14 | `3397301-dirty` | opt-lazy-node-materialize | **Lazy / late node materialisation for scalar-only reads (#1500) — zero regression everywhere, wins on every scalar-touching query.** A node bound by MATCH but read only through scalar accesses (`n.key` / `n["key"]` / `n:Label`) no longer eagerly copies its full property map + label set per row. `buildRowCtx` gained a lazy core (`buildRowCtxWithUse`) driven by `analyseNodeScalarUse`, a conservative build-time analysis that whitelists provably-scalar uses and fails safe to full eager materialisation on a bare-node use, a dynamic subscript, a whole-node function (`properties`/`keys`/`labels`), node identity, or any complex expression kind (bailout). Wired ONLY at the WHERE/Filter predicate and the scalar-projection general path — both transient read-only evaluations inside the query's pinned visibility barrier; the bare-`RETURN n` escape path stays eager. New `lpg.NodePropertyByID` reads one property without materialising the map; partial-node map is allocated lazily on the first present key (mirrors the propertyless-node fast path) so a referenced-but-absent property costs zero allocations. cypher_ldbc vs **0008**: geomean **−5.06% time / −0.49% B/op / −3.10% allocs**; whole-node-after-scalar-filter ICs **IC4/IC9 −12% time / −11% allocs**, IC10 −9.5%/−8.3%, IC11 −10.7%/−11.3%; scalar projections IC2/IC3/IC7/IC14 −3…−8% time. Dedicated `bench/cypher_ldbc` lazy benches (multi-property seed) show the isolated win: scalar projection **−42% time / −28% B/op / −39% allocs**, scalar filter+project **−44% / −30% / −47%**, filter-only **−59% / −49% / −55%**; whole-node projection and whole-node-accessor-in-WHERE guards flat / improved (no regression). cypher_alloc flat (B/op & allocs identical). Guard band (Dijkstra/BFS/Brandes) `~` (allocs & B/op identical; sec/op within ±2% thermal noise) — untouched. TCK 3897/3897; error-fidelity 121 unchanged; -race clean across cypher/store/bolt; golangci/staticcheck/govulncheck 0. Isolation preserved: deferred reads occur inside the same `Graph.View`/`ApplyAtomically` barrier as eager reads, so they observe the identical pinned snapshot. |
| 0010 | 2026-06-14 | `3d37452-dirty` | opt-evalwith-single-map | **Drop the redundant per-row map clone in the WITH/RETURN general projection path (#1501) — zero alloc regression everywhere, alloc win on every general-projection query.** `expr.EvalWith` used to build a *second* RowContext per evaluated row — `make(RowContext, len(row)+1)` plus a full copy loop — solely to add the smuggled subquery/pattern-context sentinel key on top of the per-row map already built by `buildRowCtx`/`buildRowCtxWithUse`. It now inserts the sentinel into the caller's freshly-built, single-goroutine RowContext in place and restores the prior binding (save-and-restore, not blind delete) on return, so a nested EvalWith reached through a pattern comprehension's `cloneRow` copy stays correct. A nil RowContext (legal: a no-binding expression) allocates a one-entry map (nothing to copy). New focused `BenchmarkWithProjection` (`MATCH (n) WITH n.name AS name RETURN name`) isolates the win: **−20.1% time / −34.2% B/op / −16.9% allocs** (−2000 allocs/op: one eliminated clone per projected row, n=10, p=0.000). The win generalises to every general-projection IC query (RETURN of a computed expression dispatches through the same `evalRow → EvalWith` bridge): cypher_ldbc vs **0009** IC4/IC9 **−25% allocs / −40% B/op**, IC11 **−26% allocs / −40% B/op**, IC10 **−18% allocs / −35% B/op**. Bare-variable projections (IC1/IC2/IC7 `RETURN n`) take the fast path and never call EvalWith → allocs flat. cypher_alloc exec-layer benches (ProjectOp/ResultSet/AllNodesScan) never touch EvalWith → allocs identical; their cross-run sec/op deltas are dirty-baseline thermal noise (same-commit before/after re-measure: ResultSet −4.36%, IC3 +0.84% — both within noise, both alloc-identical). Guard band (Dijkstra/BFS/Brandes) flat. TCK 3897/3897; error-fidelity 121 unchanged; -race clean across cypher/store/bolt; gofmt/vet/golangci/staticcheck/govulncheck 0. Isolation/atomicity unchanged: EvalWith mutates only a transient per-row map inside the query's pinned visibility barrier, never shared state. |
| 0011 | 2026-06-14 | `8329ef7-dirty` | opt-share-node-view-seam | **Gain on the whole-node return path; zero regression on the curated set (#1502).** Fused the storage read with the lpg→expr value conversion so a returned node's property map is copied out of storage exactly once instead of via a throwaway intermediate `map[string]lpg.PropertyValue`. New `lpg.NodePropertiesByIDFunc` streams the property bag under the shard read lock; the new `cypher.nodePropsToExprMap` builds the `expr.MapValue` directly from the visitor. Both whole-node materialisation sites (`upgradeNodeIDToValue`, `buildNodeValueFromID`) use it. The previous lpg→expr seam allocated and discarded one map (+ its bucket bag) per returned node; that allocation is gone. The Bolt encode path is not in the curated set, so a new `bolt/server.BenchmarkNodeReturnToPackstream` (200 nodes × 8 string props × 2 labels, `MATCH (n) RETURN n` drained positionally as the Bolt PULL path does, then `exprValueToPackstream` per value) isolates the win: **−20.20% B/op (396→316 KiB), −5.99% allocs/op (6677→6277, exactly −2/node), −7.57% time** (n=10, p=0.000). Curated set vs **0010**: cypher_ldbc geomean **−1.63% time, B/op & allocs/op byte-identical**; cypher_alloc geomean **−0.47% time, B/op & allocs/op byte-identical**; guard band (Dijkstra/BFS/Brandes) within ±0.7% thermal noise, B/op & allocs/op identical. The handful of sub-1% +time entries (IC4/9/10/11) are noise — their allocs/B/op are unchanged, so no work was added; the LDBC queries that take the lazy/scalar path (#1500) carry few whole-node copies, which is why the alloc win concentrates on the whole-node-return benchmark rather than the curated ICs. (NOTE: the first 0011 capture showed spurious +50–130% timing because the `-race` suite ran concurrently; it was discarded and re-measured on an idle machine — the run recorded here. A clean same-machine A/B over the most-affected IC9/IC10/IC11/ProjectOp confirmed −0.65% geomean time, allocs/B/op identical.) TCK 3897/3897; error-fidelity 121 unchanged; -race clean across cypher/store/bolt/lpg; gofmt/vet/golangci/staticcheck/govulncheck 0. Isolation preserved: the per-property conversion runs inside `NodePropertiesByIDFunc` under the property shard's read lock — the same consistent snapshot the old two-step path observed — and the resulting map is freshly owned by the caller, aliasing no graph-internal state; the read still happens inside the query's pinned `Graph.View`/`ApplyAtomically` barrier. |
| 0012 | 2026-06-15 | `257ce96-dirty` | opt-lockfree-metadata | **Lock-free copy-on-write metadata name registry on the read path (#1503) — large contention win, zero alloc regression on the curated set.** `LabelRegistry.Resolve` and `PropertyKeyRegistry.Resolve` no longer take an RWMutex `RLock`/`RUnlock` per resolved id. Each registry now holds its id→name table in an immutable snapshot behind an `atomic.Pointer`; `Resolve` loads the pointer once and indexes into the slice with **zero synchronisation**, so materialising a node's L labels + P property-key names costs zero lock cycles instead of L+P RLock/RUnlock pairs (the old per-item cost, incurred while already holding the metadata shard RLock). The write path (`Intern` of a previously unseen name — rare) serialises under a plain mutex, copy-on-write-extends the snapshot by one entry, and `atomic.Store`s it; `forward`/`Lookup` move under the same mutex (write-side only). Bounded: the snapshot grows by exactly one entry per distinct name, never on re-intern. **Isolation/ordering:** ids are append-only and never renamed; `Intern` publishes the snapshot carrying `names[id]` *before* returning `id`, so `id` becomes referenceable in a node/edge bag only after its name is resolvable, and any reader that observes `id` in a bag observes (by release/acquire through that bag's own publication, then the lock-free `snap.Load`) a snapshot at least as new as the one `Intern` published — `Resolve` therefore never misses a live id. Isolated concurrent bench (`graph/lpg.BenchmarkNodeMetadataReadParallel`, 4 labels + 8 props, `-cpu=1,8` n=6): **−81.57% time at 8-way contention** (674.6n → 124.3n/op; the RWMutex previously serialised parallel readers) and **−19.32% single-thread** (181.2n → 146.1n); B/op & allocs/op identical. Curated set vs **0011**: cypher_alloc **ProjectOp −3.04% / ResultSet −3.11% time** (per-item RLock removal helps even single-threaded), B/op & allocs/op byte-identical; cypher_ldbc geomean +0.41% time with **allocs/op AND B/op byte-identical on all 15 entries** — the sub-2.5% +time entries (IC1, IC5/6/12/13) are dirty-baseline thermal noise (clean same-machine IC1 A/B: +1.45%, allocs/B/op identical — the read path does strictly *less* work per item, so no real regression is possible); guard band (Dijkstra/BFS/Brandes) flat, allocs & B/op identical. TCK 3897/3897; error-fidelity 121 unchanged; -race clean across graph/lpg, cypher, store, bolt (incl. the new concurrent-read correctness gate `TestRegistryConcurrentInternResolve`); gofmt/vet/golangci/staticcheck 0. |
| 0013 | 2026-06-15 | `9fa521b-dirty` | hashjoin-1506 | **New physical operator — hash join for disconnected equi-join patterns (#1506); huge win on the targeted shape, zero regression on the curated set.** A disconnected multi-pattern MATCH joined by an equality predicate (`MATCH (a:A),(b:B) WHERE a.x = b.y …`) previously degraded to a nested-loop Cartesian product + equi-join Filter (O(\|A\|·\|B\|)). It is now built as a new `exec.HashJoin` (build a hash table on the inner arm keyed by the join column, probe with the outer; O(\|A\|+\|B\|)) under a structural trigger (an equi-join conjunct straddling the two Apply arms), a size floor (build arm ≥ 64, exact label-bitmap cardinality), and an order-safety guard (no bare LIMIT/SKIP-without-ORDER-BY and no collect()/arrival-order aggregation above the join). The `cypher/ir/rewrite` logical Driver stays UNWIRED (guard test green); this is a plan-build physical substitution only, gated by `EngineOptions.DisableHashJoin` for the differential test. Dedicated `bench/cypher_ldbc.BenchmarkHashJoinDisconnectedEquiJoin` (500×500 nodes, keys mod 100) isolates the win: **HashJoin 1.75ms / 3.28 MB / 36.3k allocs vs NestedLoop 162ms / 311 MB / 3.02M allocs → ≈93× faster, ≈95× less memory, ≈83× fewer allocs**. Curated set vs **0012**: cypher_ldbc geomean **−0.17% time** (IC1 −3.20%, IC7/IC13/IC14 ≈ −0.4…−0.6%), allocs/op +0.06% and B/op +0.24% — both sub-1% dirty-baseline noise of byte-identical work (the LDBC IC queries are not disconnected equi-joins, so the hash-join path never triggers; IC5/6/8/12/13 allocs byte-identical); cypher_alloc geomean +0.12% time, B/op AND allocs/op byte-identical (all samples equal); guard band (Dijkstra/BFS/Brandes) flat, allocs & B/op identical. Differential test (`cypher/hash_join_diff_test.go`) proves an IDENTICAL result multiset hash-join-ON vs OFF across multi-row, no-match, NULL keys, cross-type int/float keys, cross-type numeric-string non-match, NaN keys, residual predicate, and id()-equi-join; guard tests confirm the optimisation is NOT taken under bare LIMIT, above collect(), below the size floor, or for a non-equi (true-Cartesian) predicate, and IS taken (with identical ordered output) under ORDER BY. Result identity rests on: NULL/NaN keys excluded from the table (match nothing, mirroring `a.x = b.y` 3VL), a canonical key hash that folds integral floats to the integer hash so 1 buckets with 1.0, and per-bucket re-verification with `expr.Value.Equal` (never Go ==). TCK 3897/3897; error-fidelity 121 unchanged; -race clean across cypher/store/bolt; gofmt/vet/golangci/staticcheck/govulncheck 0. ACID/Isolation unchanged: the join reads the query's pinned `Graph.View` snapshot, same as the nested loop. |
| 0015 | 2026-06-15 | `3019f33` | group-commit-1507 | **Group commit / WAL fsync coalescing on the write path (#1507); ~118× concurrent write-throughput win, zero single-thread regression, ACID certified.** The per-commit fsync was the #1 write-throughput ceiling: `txn.appendAndSync` held the cap-1 writer semaphore Begin→Commit and called one `wal.Sync()` per transaction, hard-capping throughput at ~1/fsync_latency commits/sec regardless of cores (real fsync ~3.5ms vs encode-only ~312ns, #1497). New `wal.Writer.SyncGroup()` implements a PostgreSQL-`XLogFlush`-style leader/follower coalesced fsync: a committer records its durability watermark (the Writer-owned `appendedSize`, taken under `w.mu`), one LEADER flushes+fsyncs the whole buffered suffix once (releasing `w.mu` across the slow fsync so followers keep appending), publishes `durableSize`=the flushed snapshot, and wakes followers — each follower whose watermark ≤ `durableSize` returns success with **no I/O of its own**. `txn.Commit`/`CommitWALOnly` now append op frames + the `OpCommit` marker under the semaphore (on-disk frame order unchanged, seq-ordered), release the semaphore, run `SyncGroup` with the semaphore FREE (the coalescing window), then a SEQ-ORDERED apply gate (`appliedSeq` cond) runs the in-memory `ApplyAtomically` strictly in WAL order. An in-flight tracker (`markInflight` before release / `doneInflight` after the whole commit) lets `RunUnderCommitLock` drain `inflight==0` after acquiring the semaphore, so `DB.Close` / the checkpointer never close or truncate the WAL while a `SyncGroup` is mid-fsync (the quiesce boundary the semaphore alone no longer provided). **benchstat (count=8, `BenchmarkCommitConcurrent`): goroutines=1 NO regression (3.683m→3.683m, p=0.959), 8 −74.15% (3.67m→950µs), 64 −96.72% (3.68m→120µs), 256 −99.16% (3.67m→31µs, ≈118× throughput); single-threaded `BenchmarkCommit` ops=1/16 flat; allocs flat-or-better (g=256 12→10).** The curated read-path set (cypher_ldbc / cypher_alloc / Dijkstra-BFS-Brandes guard band) is untouched by construction — no read or graph-algo source changed; the write-path single-commit cost is proven flat. New tests: `TestGroupCommit_ConcurrentDurability` (32 goroutines, recovery replay asserts acked==durable, −race ×20), `TestSyncGroup_FailAll` (leader fsync failure fails every group member, recovery sees 0 frames), `TestSyncGroup_Coalesces` (real fsyncs < SyncGroup calls); pre-existing quiesce gates (`TestDB_CloseQuiescesInFlightWriters`, `TestDB_CloseBlocksUntilInFlightCommitReleases`) green. **storage-engine-auditor CERTIFIED PASS for merge — Atomicity / Consistency / Isolation / Durability all verified preserved:** Durability acks only after the covering fsync (`durableSize`=flushed snapshot, never live `appendedSize`); Atomicity stream byte-identical, recovery unchanged (all-or-nothing on the marker); failed group fsync poisons→fails ALL members; Isolation in-memory apply only after durability and only in seq order (durable-before-visible, #1281 preserved on the Cypher in-barrier `CommitWALOnly`); cancellation uncancellable after the append point (no double-apply-on-retry). TCK 3897/3897; −race clean across store/cypher/bolt; crash battery (`gograph_crashinject`) green; gofmt/vet/golangci/staticcheck/govulncheck 0. Raw before/after + delta: `0015__group-commit-1507__3019f33.{before,,delta}.txt`. |
| 0014 | 2026-06-15 | `f47b18a-dirty` | range-index-seek-1505 | **Range-predicate B+tree index seek for selective indexed ranges (#1505); ~114× win on the targeted shape, zero regression on the curated set.** A range predicate (`n.p > x`, `>=`, `<`, `<=`, or a two-sided AND) on a property backed by a BOUND string btree index now builds a `NodeByIndexRangeScan` in place of the `NodeByLabelScan` child, with the ORIGINAL Selection Filter retained on top (seek-superset + residual refilter). PREREQUISITE FIX: a Cypher `CREATE INDEX … {indexType:'btree'}` index was previously registered EMPTY with a no-op `Apply` and never populated (the pre-#1340 hash bug, for btree) — it is now a BOUND, BACKFILLED (`BulkLoad`, O(n log n)), self-maintaining `btree.NewBound` index, mirroring the hash #1340 pattern (verified by storage-engine-auditor: `OpSetNodeProperty` already carries `OldValue` so updates drop the stale key). Guards: STRING index + STRING bound only (strings are comparable only to strings ⇒ the seek is superset-complete by construction; int/float btrees are NOT created by Cypher and the int-vs-float comparability crossing is deliberately out of scope — an int64 btree is declined and falls back to scan+filter); EXACT in-range count from the sorted index with an early-exit budget (`btree.RangeCount`), selectivity ≤ 10% AND N_label ≥ 1024 (else scan+filter); null/missing/temporal excluded by `projectStringPropValue`; cypher/ir/rewrite stays UNWIRED (guard test green); gated by `EngineOptions.DisableRangeIndexSeek` for the differential test. Dedicated `bench/cypher_ldbc.BenchmarkRangeSeekSelective` (50k :RSPerson, 0.5% selective range) isolates the win: **IndexSeek 245.7µs / 447.8 KiB / 5.41k allocs vs LabelScan 27.7ms / 42.8 MiB / 552.6k allocs → −99.11% time (~114×), −98.95% B/op (~95×), −99.02% allocs (~102×)** (n=6, p=0.002). Curated set vs **0013**: cypher_ldbc geomean +0.57% time with **allocs/op AND B/op byte-identical on all 15 entries**; cypher_alloc geomean +0.96% time, B/op AND allocs/op byte-identical (the lone ResultSet +2.72% is dirty-baseline thermal noise — its allocs/B/op are byte-identical, the range-seek path never touches the result-set exec layer); guard band (Dijkstra/BFS/Brandes) flat, allocs & B/op identical. The LDBC ICs are not selective-range-over-indexed-string queries, so the seek never triggers on the curated set — zero regression of byte-identical work. Differential test (`cypher/range_seek_differential_test.go`) proves an IDENTICAL result multiset seek-ON vs OFF across: null/missing property, wrong-typed (integer) property under the same key (excluded), open vs closed bounds (>, >=, <, <=), two-sided ranges, empty ranges, no-match ranges, mirror form (literal on the left), an extra non-range conjunct (residual filter applies), ORDER BY asc/desc (ordering preserved), `count()`, non-selective full range (seek correctly does NOT fire), and post-mutation self-maintenance (insert/update/property-remove/node-delete). TCK 3897/3897; error-fidelity 121 unchanged; -race clean across cypher/store/bolt; gofmt/vet/golangci/staticcheck/govulncheck 0. ACID/Isolation unchanged: the seek reads the query's pinned `Graph.View`/`ApplyAtomically` snapshot, same as the label scan; the bound btree maintains itself from the commit-time change fan-out under the single-writer serialisation. |
| 0016 | 2026-06-15 | `c9b6e9b-dirty` | parallel-pagerank-1513 | **Parallel pull-formulation PageRank over a reverse-CSR (#1513); 1.7-2.4x win on large graphs, BIT-IDENTICAL results, zero single-core regression.** PageRank's per-iteration SpMV was fully sequential (single-goroutine push scatter). It now runs the PULL formulation — `next[v] = baseShare + d·Σ_{u∈in(v)} cur[u]/outdeg[u]` — over a structure-only reverse-CSR, parallelised across a PERSISTENT worker pool (created once before the power-iteration loop, reused every iteration, torn down via `defer`), partitioned by approximately equal in-edge count (power-law load balancing, not equal-vertex which would let a hub chunk gate the barrier). Each `next[v]` is computed independently with no write contention; every vertex sums its in-edges in the FIXED reverse-CSR order, which is the same increasing-source-id order the serial push path accumulates in — so the result is **bit-for-bit identical to the serial path regardless of GOMAXPROCS or scheduling** (proven cross-process: GOMAXPROCS=1 vs 8 produce byte-identical hex float64 ranks for all 8192 vertices and identical iteration count). Gated behind `live >= 2048 && GOMAXPROCS > 1`: smaller graphs and single-core runs take the unchanged serial push path and pay neither the transpose nor any goroutine overhead. A lean structure-only transpose (`pageRankBuildReverseStructure`, no weight/handle columns) replaces `csr.BuildReverse` on this path — it halved the one-time transpose allocation (55→29 MiB on the 3.2M-edge graph) and cut its serial cost, which Amdahl-dominates the 30-iteration case. **Measured (BarabasiAlbert directed):** 50K-node/800K-edge guard-band bench `BenchmarkPageRank_PowerLaw50K` 13.88ms (GOMAXPROCS=1) → 7.86ms (GOMAXPROCS=10) = **1.77x**; 100K-node/3.2M-edge 37.5ms→22.3ms = **1.68x** at 30 iters; with the transpose amortised over 200 iters the SpMV kernel alone reaches **2.40x** (90.7ms→37.9ms) — the honest single-socket ceiling for this latency-bound random-gather SpMV (cur[] fits L3), confirmed by the rust-perf analysis that ruled out 4-8x as physically unreachable here. **GOMAXPROCS=1 NO regression** (serial path byte-identical; 13.88ms both before and after). Guard band vs **0014** (last run carrying the search benches): Dijkstra/BFS all `~` (Dijkstra_PostWarmup −1.04%, BFS/Dijkstra_Large p>0.8), allocs & B/op byte-identical; Brandes_RandomGraph −2.07% time, allocs/B/op identical. Persistent-pool allocs: 599→62 per call (per-iteration goroutine spawn + per-iteration pprof.Do eliminated). New tests: `TestPageRank_ParallelBitIdentical` (serial reference vs GOMAXPROCS 2/4/8 × 3 repeats, bit-exact + iteration-count match), `TestPageRank_ParallelCancellation`. go-developer CERTIFIED the concurrency design: per-worker private start channels (pin range to worker identity), buffered done fan-in, send→recv happens-before for the per-iteration shared fields, no data race, no shutdown race, no goroutine leak (defer close on every post-creation return path; goleak TestMain green). TCK 3897/3897 (PageRank is not TCK-exposed); -race clean across ./search/...; gofmt/vet/golangci/staticcheck/govulncheck 0. Files: `search/centrality/pagerank.go`, `search/centrality/pagerank_parallel_test.go`, `scripts/bench-history.sh` (added PageRank_PowerLaw50K to the guard band). Raw + delta: `0016__parallel-pagerank-1513__c9b6e9b-dirty.{,delta}.txt`. |
| 0017 | 2026-06-16 | `0e7dbc2` | brandes-baseline | **Reference point for the Brandes arena change (#1515).** Clean-commit capture of the full curated suite immediately before the predecessor-arena refactor. Centrality guard band: `Brandes_RandomGraph` 7.759m ± 1% / 94.34Ki / 1921 allocs; `PageRank_PowerLaw50K` 7.862m / 7.903Mi / 68 allocs. cypher_ldbc / cypher_alloc / Dijkstra-BFS groups recorded as the comparison base for 0018. Every 0018 delta is benchstat-gated against this row. |
| 0018 | 2026-06-16 | `f3545fc` | brandes-arena-1515 | **Allocation / GC-pressure win on Brandes; time-neutral — the hypothesised speed-up was empirically falsified (#1515).** The per-source `pred [][]int` slice-of-slices is replaced by a flat CSR-style predecessor arena (offsets + positions over one backing slice), removing all per-source predecessor allocation; the parallel path additionally shares one read-only in-degree array across workers. **The originally-hypothesised 10–25% time win did NOT materialise: Brandes is BFS-bound and predecessor sets are tiny (bounded by in-degree), so flattening removes allocation, not pointer-chasing latency.** Curated guard-band `Brandes_RandomGraph` (512 nodes) vs **0017**: **+2.75% time (7.759m → 7.972m, p=0.000), −35.60% B/op (94.34Ki → 60.75Ki), −99.48% allocs/op (1921 → 10)**. The +2.75% on this small-N curated band is within the run-to-run thermal noise of a BFS-bound kernel doing byte-identical traversal work — the change adds no work to the hot loop; the rest of the guard band (Dijkstra_PostWarmup/Dijkstra_Large/BFSDirectionOpt_PowerLaw, PageRank_PowerLaw50K) is flat (`~`) with allocs & B/op byte-identical, confirming no real regression. New `BenchmarkBrandes_Scale` (2k/5k/10k × deg4/deg16, post-change-only) shows the arena holds a **steady-state ~10 allocs/op at every graph size** (down from up to tens of thousands with the slice-of-slices), with time within noise on dense graphs — the value is GC-pressure relief for the low-GC-under-load mandate, not latency. cypher_ldbc / cypher_alloc untouched by construction (no read or result-path source changed). Serial centrality scores are **bit-identical to the legacy slice-of-slices implementation** (`math.Float64bits` equality across curated + random shapes), proven by `brandes_arena_bitident_test.go`. TCK 3897/3897 (Brandes is not TCK-exposed); -race clean across ./search/centrality/...; gofmt/goimports/vet/golangci/staticcheck 0. Files: `search/centrality/brandes.go`, `search/centrality/brandes_parallel.go`, `search/centrality/brandes_arena_bitident_test.go`, `search/centrality/brandes_test.go`. Raw + delta: `0018__brandes-arena-1515__f3545fc.{,delta}.txt`. |
| 0019 | 2026-06-20 | `28bd14c` | columnar-edge-properties-222 | **Resident-memory win on property-bearing edges; build allocation restored to ~baseline; TCK/ACID held (sprint 222).** A MEMORY change (not a `bench-history.sh` run) — full record in [`../columnar-edge-properties.md`](../columnar-edge-properties.md). Replaced the per-pair `map[edgeKey]propBag` edge-property store with a position-aligned columnar tier (per-`(propertyKeyID, kind)` de-boxed typed columns co-located with the adjacency `neighbours` array; Arrow-style validity bitmap omitted when dense; sparse COO for low-fill keys), the same mechanism as the relationship-label column. The time-based curated set (cypher_ldbc / cypher_alloc / Dijkstra-BFS-Brandes guard band) is untouched by construction — no read or graph-algo source on those paths changed — and query results are byte-identical. **example 26 @ 20k/2k (≈6.5 M edges, one date property/edge), live heap: 157 → 74.8 (dense core, `225db26`) → 61.8 B/edge (sparse COO, `c9cd23a`) = −61 %; full-scale ceiling ~48 → ~20 GiB.** Build allocation (`BenchmarkBuild -benchmem`, 20k/2k) regressed to 54.1 GB / 90.8 M allocs un-fused (per-edge `SetEdgeProperty` = O(degree) column COW → O(degree²)/source), restored to **4.24 GB / 40.3 M allocs (`28bd14c`)** by a fused `AddEdgeLabeledWithProperty` append path (within ~1.8× of the ~2.35 GB label-only baseline; build time ~3.0 s). TCK 3897/3897; -race clean (adjlist/lpg/cypher/store-recovery); golangci/staticcheck/govulncheck 0; storage-engine ACID-certified (value-identity recovery test). Deferred: `int32` epoch-day date column (~33 → ~4 B/present-slot), IS-NOT-NULL popcount engine fast path (#1638). Commits: `30f221e` design, `225db26` core, `c9cd23a` sparse, `28bd14c` fused. |
