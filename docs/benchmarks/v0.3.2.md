# Benchmarks — v0.3.2

Headline benchmark and load-test numbers for the GoGraph **v0.3.2**
release.

> **Figures inherited from `v0.3.1`.** v0.3.2 is a **correctness-only
> PATCH**: its sole change fixes a data-compatibility recovery panic in
> `graph/adjlist.upsertEdgeLocked` (an allocation-sizing correction on
> the edge fast path) and **does not alter any hot path**. No fresh
> benchmark run was performed for this release — running one against the
> current working tree would have contaminated the figures with unrelated
> in-progress performance work. The numbers below are therefore carried
> forward **verbatim from [v0.3.1](v0.3.1.md)**, which remains the
> authoritative measured baseline for every reported path. See
> [release-notes/v0.3.2.md](../../release-notes/v0.3.2.md) for the full
> release narrative.

> Re-measure on your own hardware before relying on any absolute
> number; these are medians from documented runs, not a guarantee.

## Run environment

The environment below is the one in which the inherited `v0.3.1`
figures were measured.

| Property | Value |
|----------|-------|
| Date | 2026-06-15 |
| CPU | Apple M4 (10-core) |
| OS / arch | macOS (darwin 25.5.0), darwin/arm64 |
| Go toolchain | go1.26.4 |
| GOMAXPROCS | 10 (default) for the headline set; swept 1/8/64/256/1024 for the load tests |

## Headline benchmarks

The canonical regression-gate set, unchanged from `v0.3.1` (this patch
touches no hot path). Each figure is the **median** of the documented
`bench-history.sh` run at `-benchmem`. The guard-band group
(`Dijkstra`, `BFS`, `Brandes`) remains byte-identical within
run-to-run noise — **no regression** at this version.

| Benchmark | Package | ns/op (median) | B/op | allocs/op |
|-----------|---------|---------------:|-----:|----------:|
| `BenchmarkDijkstra_PostWarmup` | `search` | 8,049,770 (≈ 8.05 ms) | 0 | 0 |
| `BenchmarkDijkstra_Large` | `search` | 8,206,248 (≈ 8.21 ms) | 1,140,413 (≈ 1.14 MB) | 4 |
| `BenchmarkBFSDirectionOpt_PowerLaw` | `search` | 29,376,090 (≈ 29.4 ms) | 0 | 0 |
| `BenchmarkBrandes_RandomGraph` | `search/centrality` | 7,791,073 (≈ 7.79 ms) | 96,600 | 1,921 |
| `BenchmarkPageRank_PowerLaw50K` | `search/centrality` | 7,853,564 (≈ 7.85 ms) | 8,286,761 (≈ 8.29 MB) | 68 |

The two post-warmup traversal paths (`Dijkstra_PostWarmup`,
`BFSDirectionOpt_PowerLaw`) allocate **zero** bytes per call, confirming
the zero-allocation hot-path mandate still holds for the reusable-state
query forms.

## Load test — group-commit concurrent write sweep

The group-commit write path (`wal.Writer.SyncGroup`) introduced in
`v0.3.1` is unchanged in `v0.3.2`. The decisive per-commit latency
measurement under concurrency, on `BenchmarkCommitConcurrent` at
`-count=8` (inherited):

| Goroutines | Before (per-commit fsync) | After (group commit) | Delta |
|-----------:|--------------------------:|---------------------:|-------|
| 1 | 3.683 ms | 3.683 ms | flat (p = 0.959) — **no regression** |
| 8 | 3.67 ms | 950 µs | **−74.15 %** |
| 64 | 3.68 ms | 120 µs | **−96.72 %** |
| 256 | 3.67 ms | 31 µs | **−99.16 % (≈ 118× throughput)** |

## Load test — read-path concurrency sweep

CLAUDE.md requires a load-test report "at multiple concurrency levels
(1, 8, 64, 256, 1024 goroutines)". The `b.RunParallel` hot-key intern
benchmark — the tightest shared-state contention point in the read path
— is unchanged in `v0.3.2`. Inherited medians of 5 runs at
`-count=5 -benchmem`:

Command:

```
go test -run='^$' -bench='^BenchmarkMapper_Intern_HotKey_Parallel$' \
  -benchmem -cpu=1,8,64,256,1024 -count=5 ./graph/
```

| Goroutines (`GOMAXPROCS`) | ns/op (median) | B/op | allocs/op |
|--------------------------:|---------------:|-----:|----------:|
| 1 | 8.09 | 0 | 0 |
| 8 | 90.19 | 0 | 0 |
| 64 | 89.18 | 0 | 0 |
| 256 | 89.09 | 0 | 0 |
| 1024 | 88.59 | 0 | 0 |

**Reading the sweep.** From 8 to 1024 goroutines (a 128×
over-subscription of the 10 physical cores) per-op latency stays
**flat at ≈ 88–90 ns/op with zero allocations** — the structure degrades
gracefully under massive over-subscription rather than collapsing, so
the bounded-resource and lock-contention mandates hold.

## Reproduce

The same commands that produced the inherited `v0.3.1` figures:

```
# Headline / guard-band set
./scripts/bench-history.sh

# Group-commit concurrent write sweep (median of 8)
go test -run='^$' -bench='^BenchmarkCommitConcurrent$' \
  -benchmem -count=8 ./store/txn/

# Read-path concurrency sweep (median of 5)
go test -run='^$' -bench='^BenchmarkMapper_Intern_HotKey_Parallel$' \
  -benchmem -cpu=1,8,64,256,1024 -count=5 ./graph/
```

Per-change (rather than per-release) tracking lives in
[`history/`](history/) via
[`scripts/bench-history.sh`](../../scripts/bench-history.sh); the
narrative ledger is [`history/LEDGER.md`](history/LEDGER.md). The
authoritative measured baseline for every path above is
[v0.3.1](v0.3.1.md).
